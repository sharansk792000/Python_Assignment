# Python_Assignment

The return_name.py program takes a first name and last name as input, concatenates them, and prints a greeting message.

The operations.py script prompts the user to enter two numbers, performs the four basic arithmetic operations (addition, subtraction, multiplication, and division), and prints the results.
